# GitHub Actions workflow disabled — data now served live via Cloudflare Worker
# See: https://almere-underground.frankkeijzer.workers.dev
name: (disabled) Update Data
on:
  workflow_dispatch: {}
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Data is now served live via Cloudflare Worker"
